export {};

// 1. Import for user-scoped extensions (installed in user folder)
import '../../../../public/global';
// 2. Import for server-scoped extensions (installed in server folder)
import '../../../../global';

// Optional: define your own types
declare global {
    // Example: declare a custom namespace or helper type
}
